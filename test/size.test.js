states = require('../src/states.js');
Switch = require('../src/index.js');

describe('Array', () => {
  it('should return -1 when the value is not present', function() {
    s = {
      el: document
    }
    console.log(states(s).all());
    console.log(Switch);

  });
});
